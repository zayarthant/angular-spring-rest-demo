import { MagicColorDirective } from './magic-color.directive';

describe('MagicColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MagicColorDirective();
    expect(directive).toBeTruthy();
  });
});
