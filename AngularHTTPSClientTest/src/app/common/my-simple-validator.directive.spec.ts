import { MySimpleValidatorDirective } from './my-simple-validator.directive';

describe('MySimpleValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MySimpleValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
